


// This function will be used to print the ticket
// Just simulated by the moment
export function printTicket(registration, duration, created_at) {
    console.log(`${ registration } - ${ duration } - ${ created_at }`)
}
