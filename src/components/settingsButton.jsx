import { View, Text, Image, TouchableOpacity } from 'react-native'

export default function SettingsButton() {
    return(
        <View>
            <TouchableOpacity>
                
            </TouchableOpacity>
        </View>
    )
}